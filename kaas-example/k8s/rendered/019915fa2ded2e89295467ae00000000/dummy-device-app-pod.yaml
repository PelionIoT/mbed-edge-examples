apiVersion: v1
kind: Pod
metadata:
  name: dummy-device-app-019915fa2ded2e89295467ae00000000
spec:
  automountServiceAccountToken: false
  hostname: dummy-device-app
  nodeName: 019915fa2ded2e89295467ae00000000
  containers:
  - name: dummy-device-app
    image: ghcr.io/izumanetworks/kaas-example/dummy-device-app:1.0.1
    imagePullPolicy: IfNotPresent
    ports:
    - containerPort: 3000
    env:
    - name: DATABASE_URL
      value: "postgres://postgres:password@postgres:5432/device_db"
    - name: PORT
      value: "3000"
    resources:
      requests:
        memory: "128Mi"
        cpu: "100m"
      limits:
        memory: "256Mi"
        cpu: "200m"
    livenessProbe:
      httpGet:
        path: /health
        port: 3000
      initialDelaySeconds: 30
      periodSeconds: 10
    readinessProbe:
      httpGet:
        path: /health
        port: 3000
      initialDelaySeconds: 5
      periodSeconds: 5 